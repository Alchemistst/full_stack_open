note over browser:
User types text on text form and clicks the 'save' button
Default behaviour for form is overridden
Note is added to notes array
Notes are redrawn (not by reloading the whole page, but by running javascript on browser side)
end note

browser->server: HTTP POST https://fullstack-exampleapp.herokuapp.com/new_note_spa